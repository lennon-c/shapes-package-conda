from setuptools import setup

setup(
   name='shapes',
   version="0.1",
   description='An example package built using conda',
   author='carolina lennon (codio)',
   author_email='carolina.lennon@gmail.com',
   packages=['shapes'],  
   install_requires=[],
)
